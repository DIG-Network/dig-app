# Lane 3077 — wallet slice 0 (loroco parity)

QR receive, derivation index + labels, two-way activity via `control.wallet.arrivals`, arrivals/notifications wiring.
Territory: `crates/dig-app-core/src/wallet/` ONLY.

## Slice: offers — make + cancel (#3077)

Adds the two remaining offer legs on top of the merged paste/read/take path:
- **make** — pick the give leg and the want leg, get an offer string back to share.
- **cancel** — reclaim the coins locked in an offer the user made.

Both confirm surfaces name BOTH legs of the trade (#3109) and cancel is named as
destructive (NC-14 / #3079).
