**DO NOT MERGE — lane in progress; gate round has not run.**

One batch, three super-repo tickets, all in `dig-app`:

- **DIG-Network/dig_ecosystem#2953** — session lock policy: unlocked stays unlocked until `Lock now`, 24h idle, or app restart.
- **DIG-Network/dig_ecosystem#2956** — the deposit window's re-raise bound (total-drawings cap alongside the consecutive-timeout count).
- **DIG-Network/dig_ecosystem#2957** — collapse the two mojos-to-XCH renderers into one.

Anchor pushed before any reading (§1.8 push-early).
